#ifdef GL_ES
precision highp float;
#endif

varying vec2 vTexCoord;

void main() {
    gl_FragColor = vec4(0.0, 0.0, 0.15 * (1.0-vTexCoord.y), 1.0);
    gl_FragColor *= vec4(smoothstep(0.0, 0.1, vTexCoord.y));
}
