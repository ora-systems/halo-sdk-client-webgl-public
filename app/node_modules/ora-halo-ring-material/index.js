var glslify   = require('glslify-sync');


function HaloRingMaterial(ctx, uniforms) {
  this.program = ctx.createProgram(HALO_RING_VERT, HALO_RING_FRAG);
  this.uniforms = uniforms;
}

module.exports = HaloRingMaterial;
